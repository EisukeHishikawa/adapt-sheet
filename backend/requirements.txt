# ADR-018により、Docling本体とその重量級ML依存（torch/opencv-python/transformers等）は
# docling-service/requirements.txtへ分離した。ここには入口エンドポイント（backend）が
# 直接importするトップレベルパッケージのみを列挙し、詳細な推移的依存はpipの解決に委ねる。
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.4
pydantic-settings==2.11.0
python-multipart==0.0.20
httpx==0.28.1
pypdf==6.14.2  # docling-serviceへ送るPDFを1ページ目のみに切り詰めるため（純Python実装で軽量、ADR-018のtorch等重量級依存とは別枠）
google-genai==1.47.0
google-auth==2.50.0
requests==2.32.5
pytest==8.4.2
ruff==0.15.20
