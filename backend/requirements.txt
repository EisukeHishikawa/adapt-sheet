# ADR-013により、Docling本体とその重量級ML依存（torch/opencv-python/transformers等）は
# docling-service/requirements.txtへ分離した。ここには入口エンドポイント（backend）が
# 直接importするトップレベルパッケージのみを列挙し、詳細な推移的依存はpipの解決に委ねる。
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.4
pydantic-settings==2.11.0
python-multipart==0.0.20
httpx==0.28.1
pypdf==6.14.2  # docling-serviceへ送るPDFを1ページ目のみに切り詰めるため（純Python実装で軽量、ADR-013のtorch等重量級依存とは別枠）
PyMuPDF==1.24.14  # レイアウトHTML生成（app/services/pdf_layout.py、ADR-014）。純Pythonで導入でき罫線・塗りを座標付き図形として取得できる。ライセンスはAGPL/商用
google-genai==1.47.0
google-auth==2.50.0
anthropic==0.117.0  # Claude APIクライアント（ADR-015）
openai==2.46.0  # OpenAI APIクライアント（ADR-015）
requests==2.32.5
boto3==1.35.99  # Lambda本番でParameter StoreからAPIキーを取得する（app/secrets_loader.py、ADR-017）。純Pythonでwheel導入のみ
pytest==8.4.2
ruff==0.15.20
